name = "activity-eventos-totem"
compatibility_date = "2026-09-01"
pages_build_output_dir = "dist"

[placement]
mode = "smart"

# Secrets (DATABASE_URL, WHATSAPP_ACCESS_TOKEN, WHATSAPP_PHONE_NUMBER_ID)
# are stored as encrypted Cloudflare Pages Secrets — NOT here.
# To update: wrangler pages secret put <SECRET_NAME> --project-name activity-eventos-totem
